# ===================================================
# Trino Catalog: iceberg (Iceberg connector via Hive Metastore)
# ===================================================
# Mục đích: Đọc/ghi Iceberg tables với ACID, time-travel
# Use case: Query Silver/Gold layers với versioning
# ===================================================

connector.name=iceberg

# Hive Metastore catalog configuration
iceberg.catalog.type=hive_metastore
hive.metastore.uri=thrift://hive-metastore:9083

# Iceberg table properties
iceberg.file-format=PARQUET
iceberg.compression-codec=SNAPPY

# S3/MinIO configuration for accessing Iceberg data
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.path-style-access=true
s3.region=us-east-1
s3.aws-access-key=minio
s3.aws-secret-key=minio123

